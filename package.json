{
  "name": "wait-until-queue",
  "version": "1.0.0",
  "description": "wait until callback done with queue",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf ./dist && tsc -d"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:cevio/wait-until-queue.git"
  },
  "homepage": "https://github.com/cevio/wait-until-queue",
  "devDependencies": {
    "@types/node": "^14.14.12",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.2"
  }
}
